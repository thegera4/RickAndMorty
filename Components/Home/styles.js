import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  search: {
    width: '95%',
    marginTop: 10,
    marginBottom: 10,
    alignSelf: 'center',
  },

});

export default styles;