import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    padding: 16,
    borderRadius: 8,
    backgroundColor: '#fff',
    margin: 'auto',
    marginBottom: 8,
    borderStyle: 'solid',
    borderWidth: 1,
    borderColor: '#3268bd',
    width: '70%',
    height: 100,
    textAlign: 'center'
  },
  font: {
    color: 'black',
  },
});

export default styles;